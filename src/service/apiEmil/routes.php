<?php
/*
$rotas = [
  'email' => 'main@email'
];

$action = 'email';

if(isset($_GET['a'])){
  if(!key_exists($_GET['a'], $rotas)){
    $action = 'email';
  } else {
    $action = $_GET['a'];
  }
}

$partes = explode('@', $rotas[$action]);
$controller = 'src\\service\\api.php' . ucfirst($partes[0]);
$method = $partes[1];
*/
?>
